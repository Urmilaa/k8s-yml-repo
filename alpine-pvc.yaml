apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: alpine-pvc
  namespace: storage-lab

spec:
  storageClassName: standard

  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 1Gi
